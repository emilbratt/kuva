[alias]
# Skip bench targets (they pull in plotters -> system fontconfig via dev-deps)
ci-test   = "test --features cli,full --tests"
ci-clippy = "clippy --features cli,full --tests --bins -- -D warnings"
